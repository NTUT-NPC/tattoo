/// Scope of academic grade ranking comparison (排名類別).
enum RankingType {
  /// 班級排名 — ranking within the student's class.
  classLevel,

  /// 分組排名 — ranking within the student's group/division.
  groupLevel,

  /// 系(科)所排名 — ranking within the student's department.
  departmentLevel,
}
