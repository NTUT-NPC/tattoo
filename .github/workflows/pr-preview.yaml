name: PR Preview

on:
  pull_request:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'Specific branch to build from HEAD (Uses CI logic from dropdown)'
        required: false
        type: string
      commit_sha:
        description: 'Specific commit SHA to build (overrides branch)'
        required: false
        type: string

permissions:
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      build_number: ${{ steps.build-number.outputs.build_number }}
      commit_sha: ${{ steps.commit.outputs.commit_sha }}
      commit_message: ${{ steps.commit.outputs.commit_message }}
      # Note: For workflow_dispatch runs, the "create" step is skipped (it only runs on pull_request),
      # so this output may be an empty string. This is intentional; all downstream uses of
      # needs.prepare.outputs.comment_id are guarded by the same pull_request-only condition.
      comment_id: ${{ steps.create.outputs.comment-id }}
      target_ref: ${{ steps.set-ref.outputs.ref }}
    steps:
      - name: Determine Target Ref
        id: set-ref
        # Priority logic: 
        # 1. Manual SHA input
        # 2. Manual Branch input
        # 3. PR Head commit
        # 4. Default branch/commit from UI dropdown or trigger context
        run: |
          TARGET="${{ inputs.commit_sha || inputs.branch_name || github.event.pull_request.head.sha || github.sha }}"
          echo "ref=$TARGET" >> $GITHUB_OUTPUT

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ steps.set-ref.outputs.ref }}

      - name: Manage Build Number
        id: build-number
        uses: ./.github/actions/manage-build-number
        with:
          github_token: ${{ secrets.BUILD_NUMBER_PAT }}
          current_build_number: ${{ vars.build_number }}

      - name: Get commit SHA and message
        id: commit
        run: |
          echo "commit_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "commit_message=$(git show -s --format=%s)" >> $GITHUB_OUTPUT

      - name: Write Job Summary
        run: |
          echo "## Preview Builds" >> $GITHUB_STEP_SUMMARY
          echo "**Build Number:** ${{ steps.build-number.outputs.build_number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** [${{ steps.commit.outputs.commit_sha }}](${{ github.server_url }}/${{ github.repository }}/commit/${{ steps.commit.outputs.commit_sha }})" >> $GITHUB_STEP_SUMMARY
          echo "**Message:** ${{ steps.commit.outputs.commit_message }}" >> $GITHUB_STEP_SUMMARY

      - name: Find existing comment
        if: github.event_name == 'pull_request'
        uses: peter-evans/find-comment@b30e6a3c0ed37e7c023ccd3f1db5c6c0b0c23aad # v4
        id: find-comment
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: github-actions[bot]
          body-includes: <!-- pr-preview-comment -->

      - name: Create or update initial comment
        id: create
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@e8674b075228eee787fea43ef493e45ece1004c9 # v5
        with:
          comment-id: ${{ steps.find-comment.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            <!-- pr-preview-comment -->
            ## PR Preview Builds
            **Build Number:** ${{ steps.build-number.outputs.build_number }}
            **Commit:** ${{ steps.commit.outputs.commit_sha }}
            **Message:** ${{ steps.commit.outputs.commit_message }}
          edit-mode: replace

  android:
    runs-on: ubuntu-latest
    needs: [prepare]
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ needs.prepare.outputs.target_ref }}

      - name: Setup project
        id: setup
        uses: ./.github/actions/setup-project
        with:
          platform: android

      - name: Build and upload to Firebase App Distribution
        id: upload
        env:
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          MATCH_GIT_BRANCH: ${{ secrets.MATCH_GIT_BRANCH }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          FIREBASEAPPDISTRO_APP: ${{ secrets.FIREBASEAPPDISTRO_APP }}
        run: |
          bundle exec fastlane android upload_dev_firebase \
            build_number:${{ needs.prepare.outputs.build_number }} \
            pr_number:${{ github.event.pull_request.number || 'Manual' }} \
            pr_title:"${{ github.event.pull_request.title || 'Manual Build' }}" \
            commit_sha:"${{ needs.prepare.outputs.commit_sha }}" \
            commit_message:"${{ needs.prepare.outputs.commit_message }}"

      - name: Write Android Job Summary
        run: |
          echo "### Android (Firebase App Distribution)" >> $GITHUB_STEP_SUMMARY
          echo "Build **${{ needs.prepare.outputs.build_number }}** successfully uploaded to Firebase App Distribution." >> $GITHUB_STEP_SUMMARY
          echo "[Install build ${{ needs.prepare.outputs.build_number }}](${{ steps.upload.outputs.testing_uri }})" >> $GITHUB_STEP_SUMMARY

      - name: Update comment with Android build
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@e8674b075228eee787fea43ef493e45ece1004c9 # v5
        with:
          comment-id: ${{ needs.prepare.outputs.comment_id }}
          body: |
            ### Android (Firebase App Distribution)
            [Install build ${{ needs.prepare.outputs.build_number }}](${{ steps.upload.outputs.testing_uri }})
          edit-mode: append

  ios:
    runs-on: macos-latest
    needs: [prepare]
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ needs.prepare.outputs.target_ref }}

      - name: Setup project
        uses: ./.github/actions/setup-project
        with:
          platform: ios

      - name: Verify Podfile.lock is up-to-date
        run: |
          cd ios && bundle exec pod install
          git diff --exit-code Podfile.lock

      - name: Build and Upload to TestFlight
        env:
          FASTLANE_APP_IDENTIFIER: ${{ secrets.FASTLANE_APP_IDENTIFIER }}
          MATCH_GIT_URL: ${{ secrets.MATCH_GIT_URL }}
          MATCH_GIT_BRANCH: ${{ secrets.MATCH_GIT_BRANCH }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          FASTLANE_APPLE_ID: ${{ secrets.FASTLANE_APPLE_ID }}
          FASTLANE_TEAM_ID: ${{ secrets.FASTLANE_TEAM_ID }}
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY }}
        run: |
          bundle exec fastlane ios upload_testflight \
            build_number:${{ needs.prepare.outputs.build_number }} \
            pr_number:${{ github.event.pull_request.number || 'Manual' }} \
            pr_title:"${{ github.event.pull_request.title || 'Manual Build' }}" \
            commit_sha:"${{ needs.prepare.outputs.commit_sha }}" \
            commit_message:"${{ needs.prepare.outputs.commit_message }}"

      - name: Write iOS Job Summary
        run: |
          echo "### iOS (TestFlight)" >> $GITHUB_STEP_SUMMARY
          echo "Build ${{ needs.prepare.outputs.build_number }} uploaded to App Store Connect." >> $GITHUB_STEP_SUMMARY
          echo "1. Open the TestFlight app on your iOS device" >> $GITHUB_STEP_SUMMARY
          echo "2. Select \"Project Tattoo\"" >> $GITHUB_STEP_SUMMARY
          echo "3. Install build ${{ needs.prepare.outputs.build_number }}" >> $GITHUB_STEP_SUMMARY

      - name: Update comment with iOS build
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@e8674b075228eee787fea43ef493e45ece1004c9 # v5
        with:
          comment-id: ${{ needs.prepare.outputs.comment_id }}
          body: |
            ### iOS (TestFlight)
            1. Open the TestFlight app on your iOS device
            2. Select "Project Tattoo"
            3. Install build ${{ needs.prepare.outputs.build_number }}
          edit-mode: append
